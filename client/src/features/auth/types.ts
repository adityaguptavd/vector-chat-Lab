export type RegisterPayload = {
  email: string;
  password: string;
};

export type RegisterData = {
  user_id: string;
  access_token: string;
};

export type LoginPayload = {
  email: string;
  password: string;
};

export type LoginData = {
  user_id: string;
  access_token: string;
};